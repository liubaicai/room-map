version: "3"

services:
  db:
    image: postgres:12.6-alpine
    restart: always
    volumes:
      - ./db:/var/lib/postgresql/data
      - ./db-backup:/db-backup
    environment:
      - POSTGRES_DB=room-map
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
  receiver:
    build: ./room-map-receiver
    restart: always
    environment:
      - DB_HOST=db
      - DB_DATABASE=room-map
      - DB_USERNAME=postgres
      - DB_PASSWORD=postgres
    ports:
      - "9021:3000"
    depends_on:
      - db
    volumes:
      - ./wait-for-pg.sh:/wait-for-pg.sh
    command: ["/wait-for-pg.sh", "db", "node", "dist/main.js"]
  # web:
  #   container_name: room-map-web
  #   build: ./room-map-web
  #   restart: always
  #   depends_on:
  #     - db
  #   ports:
  #     - "9020:3000"
  #   environment:
  #     - DBHOST=db
